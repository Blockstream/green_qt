windows-x86_64:build:
  image: borgbyte/green_qt:windows-x86_64
  extends:
    - .build
  tags:
    - qt
    - windows-x86_64
  artifacts:
    name: "$CI_PROJECT_NAME-$CI_JOB_NAME-$CI_COMMIT_SHORT_SHA"
    paths:
      - green.exe
