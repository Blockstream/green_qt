.macos:
  variables:
    HOST: "macos"
    MACOSX_DEPLOYMENT_TARGET: 12.0
    SHA256SUM: "shasum -a 256"

.macos:staple:
  tags:
    - osxsigner
  stage: staple
  rules:
    - if: '$CI_COMMIT_REF_NAME =~ /^release_.*$/'
      when: on_success
    - when: manual
