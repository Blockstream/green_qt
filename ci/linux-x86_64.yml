linux-x86_64:build:
  image: borgbyte/green_qt:linux-x86_64
  extends:
    - .build
  tags:
    - ga
  after_script:
    - tools/appimage.sh $CI_PROJECT_DIR
  artifacts:
    name: "$CI_JOB_NAME"
    paths:
      - green
      - BlockstreamGreen-x86_64.AppImage
